# Idea Symphony 1.0 — Instruction Congruence Audit

**Date:** 2026-06-10
**Method:** Four parallel auditor agents (one per effort level: `min`, `low`, `medium`, `high`) performed read-only dry-run orchestration traces of `idea-symphony/`, walking every handoff from SKILL.md through prompts, guidance, templates, and script source, adopting both the orchestrator's and each subagent's perspective. Findings were cross-validated against the published `examples/tool-library/` sessions: where an example shows the capable orchestrator silently choosing an interpretation, the gap is **confirmed** — that silent repair is exactly what a weaker orchestrator won't perform. All high-impact citations below were independently re-verified against source lines before inclusion.

**Result:** 4 agent reports, ~110 raw findings, deduplicated to **36 quick fixes** (Section A) and **12 decision points** (Section B). No fixes were applied at audit time; fixes applied during the review discussion are recorded in the Addendum below.

**Severity key:** 🔴 high = wrong/missing artifact or blocked phase · 🟡 medium = output drift · ⚪ low = cosmetic/doc-only.

---

## Section A — Technical / Quick Fixes

Mechanical corrections with one defensible resolution. Grouped by theme. "Levels" = effort levels affected.

### A1. Cross-file contradictions (single correct side identifiable)

| # | Sev | Where | Issue | Fix | Levels |
|---|-----|-------|-------|-----|--------|
| 1 | 🟡 | `SKILL.md:441` vs `SKILL.md:161-163` | Model Selection table says "Roster planning \| Orchestrator (Sonnet)" but Step 2.1 spawns an Opus subagent. Example frontmatter (`model-reported: "claude-opus-4-7"`) confirms the subagent is the lived behavior. Table also has no row for the Haiku humanizer passes. | Row → "Roster planning (Step 2.1) \| Opus subagent \| Judgment-intensive trigger evaluation"; add "Humanizer pass \| Haiku" row. | all |
| 2 | 🟡 | `templates/brainstorm.md:61` vs `prompts/phase5_final-output.md:42-46` | Template orders the per-topic block to "quote or closely paraphrase" a categorical reframe; the prompt's *failing example* is precisely that close paraphrase ("do not preserve the noun-pair grammar"). Stale leftover from the Jun 9 reframe-demotion fix. | Rewrite template line to the prompt's own-voice rule. | low/med/high |
| 3 | 🟡 | `prompts/phase5_final-output.md:86` vs `templates/brainstorm.md:18` | Worked example cites a "1170–2345 ceiling" for a high-effort session; the actual high band is 2800–4500 (the prompt itself calls the template "the single source of truth for length"). A weak subagent anchors on the phantom ceiling and halves the deliverable. Example PLAN.md (3,272 body words, "within high-effort band") confirms the band table is current. | Recompute the example against the real band (e.g., "4,550 words, 50 over the 2800–4500 band"). | low/med/high |
| 4 | 🔴 | `prompts/phase5_final-output.md:64,66,108` + `templates/notebook-lm-instructions.md:3,237,288,299,304` vs the template's own headings (`:91,127,155,199,231` and `:87`) | NotebookLM artifact numbering is internally contradictory: per-cluster podcast episodes are "Artifact 2" in the prompt and checklist but **Artifact 3** in the canonical structure (Artifact 2 = single podcast); the checklist puts the slide-count target in "Artifact 3" though slides are Artifact 1; `:3` says "four artifact types" vs five blocks. With the flag on, a weak subagent emits episodes under the wrong artifact and runs the parity audit against the wrong sections. | Renumber every cross-reference to the heading numbering (episodes → 3, slides → 1, series parity → 3 vs 5); "four" → "five". | low/med/high (flag=yes) |
| 5 | 🟡 | `prompts/phase2A_question-gen-personas-selection.md:191` vs `guidance/phase2A:127`, `personas/the-politician.md`, `prompts/phase2C:34,172,214` | The 2.1 prompt's example output table shows Politician = **Synthesize**; everywhere else (guidance, persona frontmatter, 2C routing) Politician = **Append**. A roster copied from the example mis-routes Politician's questions on the 2C fallback path, destroying the voice-preservation the Append stream exists for. | Change the example row to `Append`. | low/med/high |
| 6 | 🟡 | `prompts/phase1_effort-level.md:59,94,113` vs `prompts/phase2C_question-synthesis.md:139-143` | Phase 1 promises "4-7 topic areas" (low/medium) and "6-9" (high); Phase 2C's engineered targets are 8-10 / 10-12 / 11-13 clusters. Example sessions (8 clusters at low, 12 at high) confirm 2C wins. | Update Phase 1's user-facing topic-area numbers to match 2C targets. | low/med/high |
| 7 | ⚪ | `guidance/phase2A_question-gen-personas.md:286-288` | "~50-80 questions → ~20-35 thematic clusters" mislabels merged-question counts as cluster counts (real cluster targets: 8-13). | "thematic clusters" → "merged questions". | low/med/high |
| 8 | ⚪ | `guidance/phase2D_brainstorming-personas.md:59` vs `SKILL.md:107` and the same file's `:254` | The guide's effort table says low = "0 (generic), no persona system"; low is actually DA + Pragmatist (the file's own catch-all section says so). | Row → "Low \| 2 \| Core pair only (no Step 2.4 selection)". | low |
| 9 | ⚪ | `guidance/phase2D_brainstorming-personas.md:1,3,11,193` + `SKILL.md:52` | The 2D guide titles itself "Persona Selection Guide **(Phase 2C)**" (2C is question synthesis); SKILL.md:52 links the 2A guidance as the "**Phase 2B** Selection Guide"; SKILL.md:204 / phase2C:30 call the Step 2.1 roster the "Phase 2B roster" while the PLAN.md heading is `## Phase 2 Step 2.1: …`. | Standardize: guide titles "(Phase 2 Step 2.4)", link text "Phase 2A Selection Guide", "Step 2.1 roster". | low/med/high |
| 10 | ⚪ | `SKILL.md:52,172` vs `prompts/phase2A:28` | "Tier 3 (5 personas)" vs the prompt/gate's "all four Tier 3 candidates" (the fifth, Connector, is handled via the Analogist swap, not as a Tier 3 candidate). | SKILL.md:52 → "Tier 3 (4 trigger-gated specialists; the Connector is handled via the Analogist swap)". | low/med/high |
| 11 | 🟡 | `prompts/min-effort-workflow.md:28` vs `:131,140-141,157` | The min workflow's own directory tree lists `SYNTHESIS.md` ("All summaries concatenated") — the file is `SUMMARIES.md`, and min explicitly produces no SYNTHESIS.md. | Line 28 → `SUMMARIES.md`. | min |
| 12 | ⚪ | `prompts/phase4_full-synthesis.md:139` | Attribution example cites `*—The Analyst*` — a retired Phase 3 persona that can never appear in `responses/`. | Swap to a selectable persona (e.g., The Systems Thinker). | med/high |

### A2. Missing handoff specifications (substitutions, inputs, isolation)

| # | Sev | Where | Issue | Fix | Levels |
|---|-----|-------|-------|-----|--------|
| 13 | 🔴 | `SKILL.md` Steps 2.2 (`:177-181`), 2.3 (`:192-220`), 2.4 (`:249-276`), Phase 3 (`:297-314`), Phase 4 (`:329-356`); `prompts/min-effort-workflow.md` Steps 1–3 | Only Steps 2.1 and 5.1 carry **Substitution:** lines. Every other spawn point hands prompts containing `{{persona_name}}`, `{{persona_slug}}`, `{{cluster_slug}}`, `{{topic}}`, `{{session}}`, `{{skill}}`, `{{current_datetime}}` with no instruction to resolve them. No file defines the display-name → slug rule (`Devil's Advocate` → `the-devils-advocate`, apostrophe dropped; PLAN table abbreviates to `DA★`), nor where `{{topic}}` comes from (PLAN.md's `## Topic Clusters (from Phase 2)` display name). | Add a Substitution line at every spawn point listing exactly that prompt's variables; add a slug-derivation rule and `{{topic}}` source to SKILL.md's Prompt conventions. | all |
| 14 | 🔴 | `prompts/phase2C_question-synthesis.md` (whole file) + `SKILL.md:192-220` | The 2.3 synthesizer branches on effort throughout (compaction targets `:139-143`, preserve-distinct gating `:91-93`, append quotas `:210-214`, `effort:` frontmatter slot `:239`) but **`{{effort}}` is never substituted into the prompt** and SKILL.md lists no effort input. Confirmed silent repair: the example's QUESTIONS.md carries the right `effort:` — the capable model inferred it from by-persona frontmatter. | Add `{{effort}}` to the prompt and a Substitution line to Step 2.3. | low/med/high |
| 15 | 🔴 | `SKILL.md:251-276` (Step 2.4) | The 2.4 subagent is never told the effort level (the 4-vs-7 algorithm and catch-all panel size depend on it), and is never given the exact PLAN.md heading string (`## Phase 2 Step 2.4: Brainstorming Personas`) that the gate (`:284`) and Phase 3 (`:307`) match on, the table format (`templates/plan.md:73-75` — which only shows a 4-persona row), or any frontmatter spec for `personas/brainstorming.md` despite `:276` requiring `model-requested` "in its output frontmatter". | Add `{{effort}}` + the verbatim heading + table format (incl. a 7-persona high example and the `99 Additional Questions` row) + a frontmatter block to the Step 2.4 instructions. (Whether to also move them to a prompt file is Decision B5.) | med/high |
| 16 | 🔴 | `prompts/phase5_final-output.md:15-26` vs `:36,48` + `scripts/build-summaries.sh:83-94` | Phase 5's Inputs list omits `synthesis/*_summary.md`, yet `:36` mandates reading the `central-tension:` frontmatter key "from every cluster's `_summary.md`" and `:48` reads their absence sections — and build-summaries.sh strips all frontmatter, so SUMMARIES.md cannot supply the keys. A subagent obeying the Inputs list literally cannot populate `## Central Tensions`. Confirmed silent repair in examples. | Add Inputs item: "`{{session}}/synthesis/*_summary.md` — read frontmatter only, for `central-tension:` (plus absence sections at min/low)". | low/med/high (min variant: Decision B2) |
| 17 | 🟡 | `SKILL.md:395` vs `prompts/phase5_final-output.md:112` + `templates/brainstorm.md:33-34` | Phase 5 substitution list is wrong in both directions: `{{skill}}` appears in the prompt (humanizer paths) but isn't listed; `{{current_datetime}}` is listed but occurs nowhere in the prompt — its real occurrence is inside `templates/brainstorm.md`, which the subagent reads off disk and no orchestrator ever substitutes (same for `{{session}}` there, and in `templates/notebook-lm-instructions.md:62-63`). Unresolved `{{skill}}` breaks the inline humanizer pass. | `:395` → resolve `{{session}}`, `{{skill}}`, `{{effort}}`, `{{current_datetime}}`; add a prompt-body line carrying the concrete session dir + datetime and stating that template-held tokens are filled from those values. | low/med/high (mirror in min if min delegates — B1) |
| 18 | 🟡 | `prompts/phase2B_question-gen_by-persona.md:11-17` + `templates/request.md:34-35` vs `SKILL.md:138` | The USER-QUESTIONS.md isolation rule exists only in orchestrator-facing docs; the 2.2 prompt contains no prohibition, and REQUEST.md (a required 2.2 input) *explicitly references* USER-QUESTIONS.md — a thorough subagent follows the pointer and biases its questions, silently defeating the isolation design. | Add to the 2.2 prompt's Inputs: "Do NOT read `{{session}}/USER-QUESTIONS.md` even though REQUEST.md may reference it — user questions are injected at Step 2.3 by design." | low/med/high |
| 19 | 🟡 | `prompts/phase2B_question-gen_generic.md:63,108` + `prompts/phase3_brainstorm_generic.md:58,105-106` | The min generic prompts name their output paths relative (`QUESTIONS.md`, `responses/...`) instead of `{{session}}/`-anchored (the min phase4 prompt anchors correctly). A subagent cwd'd at the project root writes outside the session and `split-questions.sh` exits 1. | Prefix all output paths with `{{session}}/`. | min |
| 20 | 🟡 | `prompts/min-effort-workflow.md:107,115` vs `prompts/phase4_summary-only_min-effort.md:11-13,43-53` | min Step 3's inline bullets contradict the phase4-min prompt: bullets say read the by-topic questions file; the prompt's input is only `generic-response.md`; bullets prescribe a 3-field frontmatter vs the prompt's 9-field schema. Two desynced instruction channels. | Replace the duplicated bullet lists in min Steps 1–3 with SKILL.md's pattern ("instructions and output schema live in the prompt file"), keeping only spawn count, model, substitutions. | min |
| 21 | ⚪ | `prompts/phase2B_question-gen_generic.md:72`, `phase3_brainstorm_generic.md:67`, `phase4_summary-only_min-effort.md:49` | `model-requested:` is written as a `[bracket]` subagent-fill slot, but only the orchestrator knows the value (the workflow says to inject a literal string). Convention mismatch. | Convert to `{{model_requested}}` substitution in the three prompts. | min |
| 22 | ⚪ | `templates/brainstorm.md:37` | Template frontmatter hardcodes `model-requested: "opus"` — false at min (Sonnet per the min model table). Example shows the silent repair (`"sonnet"`). | Make it a substitution/slot. | all |
| 23 | ⚪ | `prompts/phase2C_question-synthesis.md:283` | "If `[User Q]` markers appear in the input, preserve them" — no input ever contains the markers; the synthesizer itself *creates* them (`:45`). A literal reader concludes the rule is inactive and the SKILL.md:239 gate then fails. | Rewrite: "Apply the `[User Q]` marker per the USER-QUESTIONS Handling rule; ensure markers survive into the final file." | low/med/high |
| 24 | ⚪ | `prompts/phase2C_question-synthesis.md:236-243` | QUESTIONS.md frontmatter omits `session-dir:`/`datetime:` against the convention every other artifact follows (SKILL.md:46). | Add both fields (requires `{{current_datetime}}` in the 2.3 substitution line — covered by #14). | low/med/high |

### A3. Script interfaces

| # | Sev | Where | Issue | Fix | Levels |
|---|-----|-------|-------|-----|--------|
| 25 | 🟡 | `scripts/split-questions.sh:134-142` vs `prompts/phase2C:276-284` and `phase2B_…_generic.md:58-61` | The splitter keeps only `^[0-9]+\.` lines inside a cluster and **silently drops continuation lines** — but no prompt states the one-physical-line requirement. High-effort merged anchors are the longest questions in the system; a subagent that hard-wraps loses question tails in every by-topic file (and potentially trailing `[User Q]` markers), with counts still matching so no gate fires. Examples are all single-line — the capable model picked the only surviving interpretation. | Add to both prompts' format rules: "Each numbered question must be one physical line — never hard-wrap; continuation lines are dropped by the splitter." | all |
| 26 | ⚪ | `scripts/build-summaries.sh:72`, `scripts/build-synthesis.sh:72` | Both stamp `stage: "Phase 5: … Concatenation"` though SKILL.md runs them at the end of Phase 4, before the Phase 4 gate. | → `"Phase 4: …"`. | all |
| 27 | ⚪ | `scripts/build-synthesis.sh:4` vs `:32`; `SKILL.md:96`; `SESSION-STRUCTURE.md:39` | Script header comment says "matching `[0-9]*.md`" but the glob is `[0-9]*_synthesis.md`; SKILL.md/SESSION-STRUCTURE describe SYNTHESIS.md as "All summaries + syntheses" — it contains only syntheses. | Fix comment; description → "All per-topic syntheses concatenated". | med/high |
| 28 | ⚪ | `scripts/split-questions.sh:6` | Comment references `test-runs/career-change/min/...`, a repo dev path absent from the packaged skill. | Delete/replace the comment. | all |

### A4. Layout docs and resume table

| # | Sev | Where | Issue | Fix | Levels |
|---|-----|-------|-------|-----|--------|
| 29 | 🟡 | `SESSION-STRUCTURE.md:26` + `templates/synthesis-attributed.md:5` vs `prompts/phase4_full-synthesis.md:352` | SESSION-STRUCTURE names attributed files `01_operations_attributed.md`; the prompt (and examples) write `attributed/{{cluster_slug}}.md` with no suffix. The attributed template's naming note is also garbled ("`{{cluster_slug}}_[type].md` where NN is…"). A resuming orchestrator looking for `*_attributed.md` re-runs a completed Phase 4. | Fix the SESSION-STRUCTURE entry and rewrite the template naming note. | med/high |
| 30 | 🟡 | `SKILL.md:86-98`; `SESSION-STRUCTURE.md:5-41,10` | Layout omissions: SKILL.md tree lacks `SUMMARIES.md`, `questions-meta.json`, `USER-QUESTIONS.md`, `NOTEBOOK-LM-INSTRUCTIONS.md` and doesn't tag `SYNTHESIS.md` as medium/high-only; SESSION-STRUCTURE lacks `questions-meta.json` + `NOTEBOOK-LM-INSTRUCTIONS.md` and says by-persona holds "(10-19 files)" though roster math caps at 16 (10 T1 + 4 T2 + 2 T3; Connector replaces, never adds). The Step 2.2 gate explicitly licenses "move to correct location" behavior, so undocumented files risk relocation. | Add the missing artifacts (with effort/conditional tags); "10-19" → "10-16"; note in the min-differences list that `questions-meta.json` and `personas/` never exist at min. | all |
| 31 | 🟡 | `SKILL.md:416-424` | Resume table too weak: the 2.3-complete row doesn't check `questions/by-topic/` or PLAN's Topic Clusters section (produced by the separate script step — a crash between subagent and script resumes past the split); the Phase 4 row ("`synthesis/` exists") is true after the *first* file write, so a half-finished Phase 4 resumes into Phase 5 with no SUMMARIES.md; humanizer completion is recorded nowhere. And no row matches a `min` session at all (min has no Step 2.x statuses and no `questions-meta.json`) — a resuming orchestrator concludes Phase 2 never ran. | Strengthen the 2.3 and Phase 4 rows (require by-topic/ + PLAN section; require SUMMARIES.md + SYNTHESIS.md at med/high); add a humanizer line item to the PLAN status block; add a min resume sub-table mapping min's status labels. | all |
| 32 | 🟡 | `templates/plan.md:23-80` vs `SKILL.md:148,420` | PLAN.md instantiation is unspecified: the template ships with pre-filled placeholder sections for 2.1/2.4/Topic Clusters. An orchestrator that instantiates the whole template at Phase 1 creates a `## Phase 2 Step 2.4` heading containing placeholder rows — the resume condition ("PLAN.md has no 2.4 section yet") then skips Step 2.4 and Phase 3 spawns from `[Persona]` literals. Example shows minimal instantiation was silently chosen. | State in Phase 1 step 8: initial PLAN.md = frontmatter + header + Status + Notes only; later sections are appended by their producing steps. | low/med/high |
| 33 | 🟡 | `SKILL.md:278-281` + `templates/plan.md:12,20` vs `SKILL.md:131-148` | `persona-selection-review` is consumed at Step 2.4 and present in the PLAN template, but no Phase 1 step ever sets it — a template-following orchestrator emits the literal `"[auto|pause]"`. | Add to Phase 1 step 8: set `persona-selection-review: "auto"` unless the user asks to review selections. | med/high |
| 34 | ⚪ | `SKILL.md:347,369-371` | `central-tension:`'s "universal across all effort levels" statement lives only under the medium/high heading; the low section and low quality gate never mention the field (the low prompt does emit it). | Move the sentence to the shared Phase 4 preamble; add the field to the low gate. | low |
| 35 | ⚪ | `SKILL.md:301`; `prompts/min-effort-workflow.md:24-25` | Editing slips: low branch ends "…live in the prompt file. Spawn counts:" with no list; min tree says "From Introduction" for a phase named Context Gathering. | Delete the dangling label (or append "2 per topic"); "Introduction" → "Phase 1". | low, min |

### A5. Development-language remnants (dimension 8 sweep)

| # | Sev | Where | Issue | Fix | Levels |
|---|-----|-------|-------|-----|--------|
| 36 | ⚪ | `SKILL.md:194` ("all validated in SP1 iter3"), `SKILL.md:236` ("SP1 hard floors"); `SKILL.md:138` + `prompts/phase2B_…_generic.md:26` + `prompts/phase2C:47` (rationale "see CLAUDE.md" — not packaged in the .skill); `guidance/phase2A:113` ("R8: q10-15 overshoots in 50% of topics"), `:132,145,161,171,182,198` ("dev notes in repo, not in packaged skill"), `:316-318` footer; `guidance/phase2D:113` ("(B11 finding)"), `:410-411` footer; `guidance/phase1:10,397-399` ("Key Insights from Research", study-source footer); `prompts/phase1_effort-level.md:41` ("Previously the default was `medium`…"); `prompts/phase2A:49,76` ("per R6"); `prompts/phase2C:131` ("BL1 targets…" — BL1 never defined) | Research/validation history narrated inside operative files; pointers to repo paths a packaged install cannot reach. A weak subagent burns turns hunting for "SP1 iter3" or "dev notes in repo". | Strip provenance phrasing ("validated in…", "Based on:", "Last Updated", dev-note pointers, study footers); inline the one-sentence CLAUDE.md rationale; "SP1 hard floors" → "hard floors"; define or drop "BL1". The fate of phase2C's *operative* R-labels is Decision B11. | all |

---

## Section B — Decision Points

Each has multiple defensible resolutions. Recommendation first.

### B1. `min` final output: two contradictory specifications 🔴
`prompts/min-effort-workflow.md:147-163` defines a self-contained Step 4 (read QUESTIONS.md + SUMMARIES.md, content list with **no** Central Tensions / Conspicuous Absences); `prompts/phase5_final-output.md:3` claims to serve "all effort levels (`min`/…)" with different inputs and an Opus model vs min's Sonnet. The min example silently merged the two (phase5 structure incl. both sections, min's Sonnet model) — confirmed gap.
- **(a) Recommended: delegate.** min Step 4 becomes "spawn 1 **Sonnet** subagent with `prompts/phase5_final-output.md`" (substitute `{{session}}`, `{{effort}}`=min, `{{skill}}`, `{{current_datetime}}`; NotebookLM flag always absent). Delete the divergent inline content list. Matches what the good example actually did; one source of truth for BRAINSTORM.md.
- (b) Keep min self-contained but copy in the missing requirements (Central Tensions, Conspicuous Absences, REQUEST.md input). Preserves min's "no per-topic re-reads" speed at the cost of a second forked spec to maintain.
- (c) Strip `min` from phase5's header and accept a simpler min BRAINSTORM. Contradicts the template and the published example.

**Status: DECIDED — option (a), delegate (2026-06-10 review discussion). IMPLEMENTED 2026-06-11** (min Step 4 now spawns the phase5 prompt with Sonnet; divergent inline content list deleted — see Addendum 3). Supply-side verified during the discussion: all three phase4 prompts emit `central-tension:` (the min/low instructions were carried inside placeholder text; now also stated as a named guideline bullet — see Addendum).

### B2. `central-tension:` is unreachable from declared Phase 5 inputs (min variant) 🔴
`build-summaries.sh` strips frontmatter; min:151 says "do not re-read individual per-topic files"; yet phase5:36/templates require the frontmatter key. (At low/med/high this is quick fix #16; at min the re-read prohibition needs an explicit carve-out.)
- **(a) Recommended: frontmatter-only carve-out.** "Exception: read only the YAML frontmatter of each `synthesis/*_summary.md` to collect `central-tension:`; do not read bodies." Cheap, keeps the script untouched, resolves min and harmonizes with #16.
- (b) Have `build-summaries.sh` carry `central-tension:` values into SUMMARIES.md's session-level frontmatter as a list. More robust, touches the script and its consumers.
- (c) Rely on the tension's copy in each Executive Summary's opening prose and relax phase5:36's "don't scrape prose" rule at min. Weakens the authoritative-source contract.

### B3. Conspicuous Absences + `[recurring]` have no upstream substrate at medium/high 🔴
`templates/brainstorm.md:55-56` requires a session-level `## Conspicuous Absences` at every effort and phase5:48 sources it from per-cluster sections that only the **min/low** Phase 4 prompts emit; phase5:84-92's `[recurring]` weighting rules likewise reference tags only min/low define. `phase4_full-synthesis.md` contains neither (grep: 0 matches). The high example fabricated the aggregation silently — confirmed.
- **(a) Recommended: add the substrate.** Add a "Conspicuous Absences" block and the `[recurring]`/`[single]` tag rule to phase4_full-synthesis Document 3 (`_summary.md`). Symmetry across effort levels; Phase 5 instructions become true as written.
- (b) Keep Phase 4 as-is; rewrite phase5:48/88 and the template to say medium/high derive absences/recurrence from SUMMARIES.md themselves. Cheaper, but bakes in fabrication-from-aggregate.
- (c) Make the BRAINSTORM section min/low-only and drop the medium/high budget cells.

### B4. Humanizer mode (b) assembler: unowned, procedure unspecified 🔴
`SKILL.md:354` / `humanizer-pass.md:8` say "an assembler step reassembles each `_synthesis.md`… writes the file once" — passive voice; nobody is named, and nothing says how frontmatter, the `#` title, section header, `---` rules, and the `**Questions addressed**` footer survive the split/rejoin. Dropping frontmatter corrupts the file right before `build-synthesis.sh` reads it.
- **(a) Recommended: orchestrator splices deterministically.** Spec in SKILL.md: orchestrator splits on `^### Question \d+` keeping prefix/suffix verbatim, fans out blocks, rejoins prefix + humanized blocks + suffix, single Write. No extra spawn; modest orchestrator context.
- (b) One Haiku assembler subagent per file, handed all blocks. Keeps orchestrator context minimal (consistent with the architecture doctrine) but adds an LLM step to a mechanical join.
- (c) A deterministic split/join script pair in `scripts/` (consistent with the repo's script philosophy; most work).

### B5. Step 2.4 has no prompt file 🔴
Every other spawn uses a prompt file; 2.4's instructions are inline in SKILL.md and incomplete (see quick fix #15 for the must-add content regardless). The high example's PLAN.md actually **lacks** the exact 2.4 heading and references a nonexistent file — even the capable orchestrator fumbled this handoff.
- **(a) Recommended: create `prompts/phase2D_brainstorm-personas-selection.md`** mirroring the 2A prompt pattern (inputs incl. `{{effort}}`, verbatim output templates for both files, frontmatter block, catch-all rule). Consistency makes the weakest link match the strongest.
- (b) Keep inline but fold in everything from #15. Less churn; SKILL.md grows toward its 500-line ceiling.

### B6. Default effort level: `medium` vs `low` 🟡
`SKILL.md:111` (+ repo CLAUDE.md) say default `medium`; `prompts/phase1_effort-level.md:39-41,141` says suggest/default `low` and narrates the change ("Previously the default was `medium`…"). Same request → different pipeline depending on which file the orchestrator trusts.
- **(a) Recommended: `low` everywhere.** The prompt reads as the deliberate later decision with stated rationale; update SKILL.md:111, CLAUDE.md, and check `examples/CHOOSING-AN-EFFORT-LEVEL.md` for a default claim.
- (b) `medium` everywhere (revert the prompt). Matches the older docs and the "Default to medium" muscle memory.
- (c) Split semantics: `medium` when the user says nothing at all, `low` as the no-trigger *suggestion* — but then state it identically in both files.

### B7. `questions-meta.json` numbering scheme 🟡
phase2C's file template restarts question numbering at 1 per cluster; its sidecar schema example uses global ordinals (`[45,46]` for additional questions). The two published examples diverged: the low session invented `"C1Q1"` compound IDs; the medium/high sessions used global ordinals — direct proof two capable runs read it differently.
- **(a) Recommended: document the dual scheme explicitly** — "QUESTIONS.md restarts per cluster; all `question_numbers` in the sidecar are global ordinals counted top-to-bottom." Matches the schema example and the medium/high precedent; zero format change.
- (b) Global numbering in QUESTIONS.md too. Simplest mapping, but changes the user-visible file and per-cluster files.
- (c) Compound IDs (`"C5Q4"`) in the sidecar. Self-describing, but invalidates the schema example and the medium/high examples.

### B8. "Grep-based verification" of humanizer output is named but never defined 🟡
Asserted in four places (`SKILL.md:335,356`, `humanizer-pass.md:64`, `min-effort-workflow.md:124`); no patterns, thresholds, or record location anywhere. The gate is unenforceable as written.
- **(a) Recommended: define one concrete checklist** in SKILL.md's Phase 4 (e.g., every framing-marker prefix present pre-pass still present post-pass; per-file em-dash count decreased or held; bold-lead count per `### Question N` block unchanged; log results in PLAN.md Notes; re-run the file's pass on failure).
- (b) Downgrade the language to "spot-check".
- (c) Delete the claim and trust self-report.

### B9. `guidance/phase2D_…_max-option.md` is unreachable 🟡
"max" exists as an effort level nowhere; zero references to the file repo-wide; it shares the identical (wrong) title with the live 2D guide and **lacks the Catch-All Cluster section** — a 2.4 subagent globbing `guidance/phase2D*` can load the wrong file and never learn the fixed-panel rule for `99_additional`.
- **(a) Recommended: move it out of the packaged skill** (to `docs/dev/`). Removes the glob hazard; preserves the design work.
- (b) Implement `max` as a real routed effort level (and add the catch-all section to the clone).
- (c) Keep in place but retitle with a "not routed — do not load" banner. Weakest protection.

### B10. `scripts/count-cluster-questions.py` is an orphan 🟡
Referenced by nothing; no session-dir argument (hardcoded cwd-relative `open('QUESTIONS.md')`, unlike all three shell scripts); its regex never counts `## Additional Questions`, so its total disagrees with split-questions.sh whenever the catch-all exists.
- **(a) Recommended: delete from the packaged skill** (or relocate to repo dev tooling). The Step 2.3 gate currently works without it.
- (b) Promote it into the Step 2.3 quality gate: add `<session-dir>` argv, count the Additional section, reference it from SKILL.md:232-241. Gives the gate a mechanical tool, at the cost of new wiring.

### B11. phase2C's R-label research tags: strip or formalize 🟡
`prompts/phase2C` weaves R1–R12 labels through its instruction headers ("(R8, with R11 source-bound carve-out)"). Some are load-bearing internal cross-references (the R5 audit, `r11_source_bound_cells` in the sidecar schema); the provenance framing ("tightened by R9") is dev narration. SKILL.md:236's "SP1 hard floors" points at them.
- **(a) Recommended: keep R-labels as named rule IDs but add one definition line** ("R-numbers are internal rule identifiers used for cross-reference within this prompt") and strip only the provenance verbs ("validated/tightened by"). Preserves working cross-references incl. the JSON field name.
- (b) Strip all R-labels and rename the sidecar diagnostics. Cleanest read; touches the meta-JSON contract and any tooling that greps it.

### B12. `min` Session Index pruning rule ⚪
Three specs disagree: `templates/brainstorm.md:75` unconditionally lists `questions/by-persona/` (dead link at min) while `:28`/phase5:100 say only the SYNTHESIS.md line is conditional; min:157 ("list only SUMMARIES.md") read literally also deletes the `synthesis/` line the template keeps. The min example pruned even further (also dropped `by-topic/` and `responses/`, which *do* exist) — confirmed ambiguity.
- **(a) Recommended: minimal rule** — at min, delete exactly the lines whose targets don't exist (`questions/by-persona/`, `SYNTHESIS.md`); keep everything else; reword min:157 accordingly.
- (b) Codify the example's deeper pruning (index only REQUEST/PLAN/QUESTIONS/SUMMARIES/synthesis/). Tidier min deliverable; hides real artifacts.

---

## Coverage notes

- All four recent change areas got findings: catch-all cluster (B9 — the max-option clone lacks the rule; flowcharts confirm `99_additional` routing is otherwise consistent), humanizer post-step (#16/B2/B4/B8, mode-(c) ownership quick-fixed via `humanizer-pass.md:5` rewording (**correction 2026-06-11:** the companion SKILL.md Step 5.1 mention recorded here never landed — grep finds no humanizer text in Step 5.1 in any commit; re-filed as #50 in Addendum 2), `{path}` → `{{path}}` and the `{slug}`/`{cluster}` third-convention strays folded into #13's convention cleanup), central-tension (#16/#34/B2), NotebookLM addon (#4/#17).
- Known-intentional items excluded as instructed: 19-of-23 question-generation persona pool; `99_additional` as a real synthesized cluster.
- Tier 3 at `low` (guide says medium/high-only; prompt/SKILL.md:52 allow 0-1 strong-trigger at low; SKILL.md:107's "10 Tier 1 personas" implies none) is a residual inconsistency: recommend allowing **0-1 strong-trigger at low** (matches the 2.1 prompt's explicit Low section) and updating `guidance/phase2A:79,124-128` + adding the missing Low column to the prompt's Volume Ranges table (`phase2A:286-307`) + SKILL.md:107. Filed here rather than as a full decision point because the prompt — the file the subagent actually executes — is unambiguous; the guide is the stale side.
- The high-effort selection algorithm never pins the panel at exactly 7 (`guidance/phase2D:213-235` allows 6 or 8); add a final "count and reconcile to exactly 7" step — quick fix, folded into the B5 work.

---

## Addendum (2026-06-10): Horizontal congruence pass — Phase 4 summary specs

### The methodology gap the vertical audit missed

The four effort-level traces each validated producer→consumer chains *within one path* (prompt → artifact → downstream reader). None of them diffed **siblings** — the files that play the same role at different effort levels. Same-role drift is invisible vertically because each variant is internally consistent with its own consumers; it only appears when the variants are laid side by side. The Executive Summary / central-tension instruction divergence across the three `phase4_*` prompts (found manually during the B1 discussion) was exactly this class; a deliberate horizontal pass over the four summary-spec files (`templates/synthesis-summary.md` + `phase4_summary-only_min-effort.md` + `phase4_summary-only_low-effort.md` + `phase4_full-synthesis.md`) then surfaced 15 further items.

### Fixed in this pass (2026-06-10, review session)

| Item | Resolution |
|---|---|
| Executive Summary / central-tension instruction divergence (three idioms, three wordings) | Canonical **Central tension** guideline bullet (shared invariant wording + level-specific additions) + lean template slot + terse frontmatter placeholder ending "Phase 5 reads this key" in all three prompts and the template |
| Action-list heading drift ("Recommended Actions" vs "Recommended Next Steps") | Unified to "Recommended Actions and Next Steps" in all four files; template's now-obsolete "Section heading variant" section deleted |
| full D3 action count accidentally flattened to 4-8 during the heading harmonization | Restored to 6-10 (template's scaling note was already correct) |
| `stage:` value split — "Phase 4: Summary Generation" (min/low) vs "Phase 4: Response Synthesis" (med/high) | Harmonized to `"Phase 4: Response Synthesis"` everywhere; template's "Frontmatter variants" section deleted (it was also stale re `synthesis-type`, now emitted at every level) |
| Footer second-line key drift (**Response sources** / **Source lenses** / **Key insights synthesized**) | **Response sources** at all levels; med/high adds **Key insights synthesized** as a third line; template documents both |
| H1 slot label drift (`[Topic Cluster Name]` vs `[Topic Name]`) | Standardized to `# Summary: [Topic Cluster Name]` |
| Em-dash budget present only in full-synthesis | Copied into the min and low prompts |
| min prompt read no `REQUEST.md` (low and full both do) | Added as min input 1 |
| full D3 had no document-level word target (min/low: 500-800) | Added "800-1500 words total"; template now records both values under a new "Document length" note |
| Template low-variant Key Considerations used persona-named labels the low prompt bans | Fixed to lens-typed labels ("the two lenses disagreed", "[Flag — risk-oriented \| feasibility-oriented]"). *Numbering note: in discussion this was loosely cited as "quick fix #20" — that was the **low agent's raw** finding number. Section A **#20** (min Step 3 bullets desync) is a different item and remains open.* |
| Template silent on the per-level action-item tag systems | Tag-vocabulary note added to the action-list scaling note |
| Template's "Authoritative source" link display names didn't match the real filenames | Fixed |
| min theme-slot double-bracket typo | Fixed |
| `synthesis-type: "summary"` absent at min/low | Added (user edit, same session) |

### Open to-dos from this pass

1. **Template omits two real Key Considerations subsections** — min's "Conspicuous absences" and low's "Neither-lens gaps", both Phase-5-consumed. Add to `templates/synthesis-summary.md` (canonical-block note + low variant).
2. **full D3 has no tag/through-line discipline** — min uses `[recurring]`/`[single]` + the through-line rule; low uses `[convergent]`/`[trade-off]`/`[unique:]`; full has none, while phase5:84-92 weights `[recurring]` items from "any cluster's `_summary.md`". Resolve inside Decision **B3** — it travels with the missing Conspicuous Absences substrate at medium/high.
3. **Recovered finding (dropped in the original dedup):** `templates/synthesis-attributed.md` and `templates/synthesis-merged.md` use `### [Short Question 1 Summary]` headings, plain description lines, and `*[Persona Name]*` attribution, while phase4_full-synthesis Documents 1-2 (and the published examples) use `### Question N: [Short Question Summary]` + `**Full question**:` + `*—The Visionary*`. Load-bearing: humanizer mode (b) chunks `_synthesis.md` on `^### Question N`. Align both templates to the prompt.

### Remaining horizontal targets (same technique)

**All six targets run 2026-06-11 — see Addendum 2 for findings.** None came back clean.

- ~~`prompts/phase2B_question-gen_by-persona.md` vs `prompts/phase2B_question-gen_generic.md`~~ → A6 (#37–42), B13
- ~~`prompts/phase3_brainstorm_by-persona.md` vs `prompts/phase3_brainstorm_generic.md`~~ → A7 (#43–46), B14, B15
- ~~The four per-effort paths through `prompts/phase5_final-output.md` + `templates/brainstorm.md`~~ → A8 (#47–48), B16
- ~~Humanizer call sites across all four levels~~ → A9 (#49–53), B4 amendment
- ~~`prompts/phase1_effort-level.md` vs `guidance/phase1_effort-level.md`~~ → A10 (#54–58), B17
- ~~`templates/synthesis-attributed.md` / `synthesis-merged.md` vs phase4_full-synthesis Documents 1-2~~ → A11 (#59–64)

---

## Addendum 2 (2026-06-11): Horizontal congruence pass — remaining six targets

**Method:** Six independent auditor agents, one per target from Addendum 1's remaining-targets list, each given the horizontal technique (side-by-side sibling diff, classify intentional-scaling vs drift vs gap), the known-intentional list, and the full text of already-filed findings to exclude. 43 raw findings returned; 1 dropped as a duplicate of filed #35 (the exclusion lists otherwise held — no other re-reports), 4 merged or folded into existing items, and every surviving 🔴/🟡 citation was re-verified against source lines before inclusion. **Result: 28 new quick fixes (#37–64), 5 new decision points (B13–B17), one amendment to B4, and scope extensions to #1, #6, #17, #21, B3, B6.** No fixes applied; same protocol as the original audit.

Notable: the pass caught an error in this audit's own record — the Coverage notes claimed the mode-(c) SKILL.md Step 5.1 mention was already fixed; it never landed (#50, coverage note corrected in place).

### Section A6 — Phase 2B question-gen siblings (quick fixes)

| # | Sev | Where | Issue | Fix | Levels |
|---|-----|-------|-------|-----|--------|
| 37 | 🟡 | `prompts/phase2B_question-gen_by-persona.md:21` vs `prompts/phase2C_question-synthesis.md:182,199` | By-persona prompt mandates "3–5 thematic clusters" for every persona, but Perspective personas generate only 5-8 questions (3 at low, post-B13) — at that volume the rule forces single-question buckets, which the prompt's own rationale ("a single bucket is not a cluster") forbids. 2C expects and tolerates 2-4 internal clusters ("personas with ≤4 questions that produce 2 clusters"). Confirmed: all four low-effort tool-library Append personas emitted 2 clusters, silently violating the stated rule. | Scale the rule: "2–3 clusters when your volume is ≤8; 3–5 above that." | low/med/high |
| 38 | 🟡 | `prompts/min-effort-workflow.md:43-44,47` vs `prompts/phase2B_question-gen_generic.md:20,23-24` | min Step 1's inline bullets restate the generic prompt's USER-QUESTIONS rules and have drifted: "expand/refine them" contradicts the prompt's "preserve verbatim or with minimal refinement"; the overlap-consolidation rule ("Do NOT keep both") is absent; "(plus any user-provided questions)" reframes user questions as additive rather than integrated-with-overflow. Same desynced-duplicate-channel pattern as filed #20 (which covers min Step 3); this is the Step 1 instance. Confirmed: the min example preserved user questions verbatim per the prompt file, ignoring the spawn site. | Collapse Step 1's instruction bullets to the SKILL.md Step 2.2 delegation pattern ("instructions, USER-QUESTIONS handling, and output schema live in the prompt file"), keeping only spawn mechanics; anchor the surviving `QUESTIONS.md` mention as `{{session}}/QUESTIONS.md` (re-anchors filed #19 at the spawn site too). | min |
| 39 | 🟡 | `prompts/phase2B_question-gen_by-persona.md:64`, `prompts/phase3_brainstorm_by-persona.md:70`, `prompts/phase4_full-synthesis.md:116,179,236` | `effort:` frontmatter is a bare `[low\|medium\|high]` subagent-fill slot with no information channel: REQUEST.md deliberately omits effort (`templates/request.md:40`), PLAN.md is either not an input or scoped to the roster table, and unlike its neighbor fields the slot carries no provenance comment. The generic siblings hardcode the value (`…generic.md:70` `"min"`); 2A and Phase 5 use orchestrator-substituted `{{effort}}` (SKILL.md:165,395). Load-bearing via #14's confirmed silent repair: 2C infers session effort from by-persona frontmatter. Confirmed: example files carry correct values with no documented source. | Change all five slots to `effort: "{{effort}}"` and add `{{effort}}` to the Step 2.2 / Phase 3 / Phase 4 Substitution lines (folds into #13's spawn-point work). | low/med/high |
| 40 | ⚪ | `prompts/phase2B_question-gen_by-persona.md:54` vs `:55` | "Put your persona name at the top of the document" contradicts "starting with the YAML frontmatter" — satisfied literally, the persona name lands above the `---` and breaks frontmatter parsing. The template's H1 already carries the name. Generic sibling has only the clean formulation. | Delete line 54 (or reword to "your persona name appears in the H1, as shown in the template"). | low/med/high |
| 41 | ⚪ | `prompts/phase2B_question-gen_by-persona.md:16` vs `:69-70` | "Copy the `category` and `stream` values … verbatim" from persona frontmatter (unquoted: `category: structural`) vs the output template's quoted slots (`category: "[analytical\|…]"`). The two rules cannot both be satisfied. Confirmed: examples emit unquoted values ("verbatim" won). | Drop the quotes from the two template slots. | low/med/high |
| 42 | ⚪ | `SKILL.md:185` vs `prompts/phase2C_question-synthesis.md:17` | Step 2.2 gate checks `stream` and `category` frontmatter but not `volume`, which 2C names as a third routing-relevant field (and uses in compaction arithmetic); 2C's fallback covers only missing `stream`. | Gate → "includes `stream`, `category`, and `volume` fields." | low/med/high |

### Section A7 — Phase 3 brainstorm siblings (quick fixes)

| # | Sev | Where | Issue | Fix | Levels |
|---|-----|-------|-------|-----|--------|
| 43 | ⚪ | `prompts/phase3_brainstorm_by-persona.md:58,80-96` vs `prompts/phase3_brainstorm_generic.md:54,74+` | Both siblings share the sentence "No numbering, dividers… beyond what's shown below," but the templates show opposite divider conventions: generic has `---` between question blocks, by-persona has none. Confirmed: published by-persona outputs use inter-question dividers everywhere (the example DA file has 5 the template forbids), so spec and de-facto format disagree, and any consumer splitting on `---` segments differently per effort level. | Add the inter-question `---` to the by-persona template (matches lived output). | low/med/high |
| 44 | ⚪ | `prompts/phase3_brainstorm_generic.md:14` vs `phase3_brainstorm_by-persona.md:11-19` | The "use Glob to resolve the filename" fallback for imperfect cluster references exists only in the generic sibling — yet the by-persona path (3 spawn variants × per-persona fan-out, no slug-derivation rule per #13) is where slug errors are most likely. | Copy the one-sentence Glob fallback into by-persona input 3. | low/med/high |
| 45 | ⚪ | `prompts/phase3_brainstorm_generic.md:48` vs `phase3_brainstorm_by-persona.md:23,84-96` | Both prompts state the 3-5 response range, but only generic's template shows how it's exercised ("Add `Response D`/`Response E` only when a question genuinely supports more angles"); by-persona's template shows exactly three bullets with no expansion cue — a template-imitating subagent flattens to 3 everywhere, collapsing the range high effort is meant to exploit. | Add the optional-D/E line to the by-persona template. | low/med/high |
| 46 | ⚪ | `prompts/min-effort-workflow.md:79-80` vs `prompts/phase3_brainstorm_generic.md:11-13` | min Step 2's inline paraphrase demotes REQUEST.md to "read … for context (if needed)" and inverts the read order; the prompt file lists REQUEST.md as the first mandatory input. Third instance of the min spawn-site duplication pattern (#20, #38). | Drop "(if needed)" or replace Step 2's list with the delegation pattern. | min |

### Section A8 — Phase 5 per-effort paths (quick fixes)

| # | Sev | Where | Issue | Fix | Levels |
|---|-----|-------|-------|-----|--------|
| 47 | 🟡 | `prompts/phase5_final-output.md:37,41` vs `templates/brainstorm.md:12,17` | The prompt's only drafting-time length guidance for Session Overview and per-topic blocks is a flat "2-4 sentences" (≈45-100 words) — below the template's medium/high floors (Overview 80/100; per-topic 120/150 per block), which the prompt itself calls "the single source of truth for length." A weak model drafts to the sentence count, then audit (a) flags every block under floor with no tiebreak. Confirmed: tool-library high Session Overview = 85 words (floor 100), first per-topic block = 88 (floor 150); youth-mentorship high first block = 114 (floor 150). | Replace both flat counts with pointers to the budget-table rows for `{{effort}}` (or effort-scale them: "2-4 sentences at min/low; 4-8 at medium/high"). | med/high |
| 48 | 🟡 | `templates/brainstorm.md:83-84` vs `prompts/phase5_final-output.md:98,100` | The Session Index's two italic annotations get opposite treatment with no rule distinguishing them: line 83's *(omit at `min`/`low`)* is orchestration metadata duplicated as the prose rule at phase5:100, but line 84's *(`medium`/`high` only)* has no rule anywhere — and phase5:100's "Keep all other lines as-is at every effort level" literally commands emitting it. At min/low the kept line also advertises `_synthesis.md`/`attributed/`, which don't exist there. Confirmed: 31/40 example BRAINSTORM.md files emit line 84's annotation verbatim (including 11 min/low sessions); 0/40 leaked line 83's — direct evidence of the asymmetry. | Strip both italic annotations from the canonical block; carry all effort conditionality as prose rules beside phase5:100, adding: at min/low, end the `synthesis/` line after "`_summary.md` (always)". | all |

### Section A9 — Humanizer call sites (quick fixes)

| # | Sev | Where | Issue | Fix | Levels |
|---|-----|-------|-------|-----|--------|
| 49 | 🟡 | `prompts/phase5_final-output.md:112` vs `prompts/humanizer-pass.md:5` and `prompts/min-effort-workflow.md:163` | The low/med/high mode-(c) call site never names its mode ("run an inline humanizer pass") while min:163 and both SKILL.md mode (a)/(b) sites do — and humanizer-pass.md:5's "The orchestrator will tell you which mode you are in" is false for the Phase 5 subagent self-applying the brief. A weak subagent can bind to mode (b)'s "do **not** write any file / return the block as your final message" semantics and leave BRAINSTORM.md un-humanized while reporting success. | phase5:112 → "…an inline humanizer pass (**mode (c)**, whole-file pass)…"; humanizer-pass.md:5 → "Your caller (the orchestrator, or the Phase 5 subagent applying this brief to its own output) will tell you…". | low/med/high |
| 50 | 🟡 | `SKILL.md:385-405` (Step 5.1) vs Coverage notes above + `prompts/humanizer-pass.md:64` | **Phantom fix.** This audit's coverage note recorded mode-(c) ownership as "quick-fixed via SKILL.md Step 5.1 mention + humanizer-pass.md:5 rewording" — the humanizer-pass.md half landed, the SKILL.md half never did (grep: zero humanizer mentions in Step 5.1, in any commit per git pickaxe). The orchestrator's view of Phase 5 therefore omits mode (c) entirely: it cannot perform the verification humanizer-pass.md:64 assigns it ("performed separately by the orchestrator") because no SKILL.md step tells it BRAINSTORM.md was humanized, and resume logic stays humanizer-blind (compounds #31). | Add one sentence to Step 5.1: "The prompt's final step is an inline humanizer pass (mode (c)) over `BRAINSTORM.md`, run by the same subagent; the orchestrator verifies the result (see Phase 4 humanizer post-step)." (Coverage note corrected in place, 2026-06-11.) | low/med/high |
| 51 | 🟡 | `prompts/humanizer-pass.md:3` | "Apply the humanizer skill at `{{skill}}/prompts/humanizer/SKILL.md`" — in modes (a)/(b) the file is a spawned prompt and gets substituted, but in mode (c) the Phase 5 subagent reads it **off disk**, so `{{skill}}` survives literally; the subagent globs a nonexistent path and silently degrades the pass to the brief's short goal list. Same template-held-token class as #17, in a file #17 doesn't name. | Reword to a self-relative reference: "the humanizer skill at `humanizer/SKILL.md`, sibling to this file." | low/med/high |
| 52 | ⚪ | `prompts/min-effort-workflow.md:184` (+ pending fix for #1) | The model table's single "Humanizer post-pass \| Haiku" row covers only the mode-(a) fan-out; min's second humanizer invocation (mode (c) at :163) runs inline on the Step 4 agent. Applying #1's fix as drafted ("add Humanizer pass \| Haiku row" to SKILL.md) would bake the same overgeneralization into SKILL.md, contradicting the inline-on-Opus reality at three levels (confirmed: youth-mentorship high PLAN records the inline pass on Opus). | min:184 → scope row to "(`_summary.md` files)" and add an inline-pass row "(same agent as Final output)"; write #1's new SKILL.md row as "Humanizer pass, modes (a)/(b) \| Haiku" with a mode-(c)-runs-inline note. | min (+ all via #1) |
| 53 | ⚪ | `prompts/humanizer-pass.md:48` vs `SKILL.md:335`, `prompts/min-effort-workflow.md:32` | The load-bearing-reframe exception ("unless the cluster's `_synthesis.md` marks that one…") is keyed to a file that does not exist at min/low — the two levels where mode (a) runs alone. A literal Haiku subagent globs for `_synthesis.md`, finds nothing, and breaks up unprefixed reframes the `_summary.md` itself flags. | Reword to source-relative: "unless the source document marks that one as a load-bearing reframe (a framing-marker prefix is the signal)." | min/low |

### Section A10 — Phase 1 prompt vs guidance (quick fixes)

| # | Sev | Where | Issue | Fix | Levels |
|---|-----|-------|-------|-----|--------|
| 54 | 🔴 | `guidance/phase1_effort-level.md` (whole file) vs `prompts/phase1_effort-level.md:5-9,25-30,76-79,113-116,173-176` | **The guidance is a pre-`min`, three-level document.** Its "(low, medium, high)" framing (`:3`), decision tree (`:141-159`), and Quick Reference have no `min` branch; its "Low" carries `min`'s triggers nearly verbatim (`:262-269` = prompt:25-30), `min`'s numbers (`:52-53` "15-20 questions across 3-5 topics" = prompt:76-79), and `min`'s persona profile (`:389` "Low: 1 generic voice (no personas)" — the real `low` is DA + Pragmatist); its example tables route `min`-shaped tasks to Low (`:312` birthday party → Low vs prompt:175 → `min`). The prompt actively routes users into it (prompt:139; SKILL.md:119): an orchestrator following that pointer presents a menu contradicting the recommendation it just made, or routes a checklist request to `low` at ~3.3× the cost. Confirmed: examples contain real four-level sessions; the `low` sessions match the prompt's `low`, not the guidance's. | Restructure to four levels: rename the current "Low" content to `min`; write a genuine `low` section (32-42 Qs, 2 brainstormers/topic, summary-only synthesis); add `min` to the decision tree, Quick Reference, and example tables; rebase the "Key Insights from Research" section on the 2026-06 four-level comparison data (beyond #36's provenance sweep). | all |
| 55 | 🟡 | `guidance/phase1_effort-level.md:87,123,116,135,189-194,391` vs `prompts/phase1_effort-level.md:8-9,59,95`, `SKILL.md:108-109` | Every brainstorming-persona number in the guidance is one generation behind: "3 (visionary, analytical, pragmatic)" vs medium's 4/topic; "5 personas" + "5/5 personas agreed" convergence math vs high's 7/topic; the named trio matches no actual selection mechanism (Core pair + rings). An orchestrator quoting it under-describes the session or flags a correct 7-file response directory as over-spawn. Confirmed: example sessions have 4 (medium) and 7 (high) persona files per topic. | 3→4, 5→7, "N/5"→"N/7"; replace the trio with the concentric-circles description; fix the `:388-393` FAQ (Low: 1 generic voice → `min`; add `low`: 2 personas). | low/med/high |
| 56 | 🟡 | `guidance/phase1_effort-level.md:86,122` vs `prompts/phase1_effort-level.md:58,94`, `SKILL.md:108-109` | Question counts stale: guidance says 25-35 (medium) / 40-60 (high) vs the prompt's 45-65 / 55-90 — ~40% understated; usable as a false sanity check that trims healthy runs. Confirmed: example sessions have 49 (medium) and 71 (high) questions, inside the prompt's ranges and outside the guidance's. | Update to 45-65 / 55-90, sourced from the same place as the #6 fix. | med/high |
| 57 | ⚪ | `guidance/phase1_effort-level.md:266-269` vs `prompts/phase1_effort-level.md:27,30` | Even after #54's relabel, the guidance trigger list would remain a stale subset: missing "step by step" (unhyphenated), "guide to", "show me how". (HIGH/MEDIUM trigger lists are verbatim-identical — clean.) | Copy trigger lists from the prompt verbatim during the #54 rewrite, or make the guidance defer to the prompt as the single source for triggers. | min |
| 58 | ⚪ | `guidance/phase1_effort-level.md:54,88,124` vs `examples/CHOOSING-AN-EFFORT-LEVEL.md:17` | "Output size" KB claims (10-15/20-25/25-35 KB) come from the superseded three-level study; measured deliverables run ≈13/17/19/22 KB across the four levels, and the "Low" row describes the mislabeled level. | Replace with measured word counts or link the CHOOSING guide's table. | all |

### Section A11 — Synthesis templates vs full-synthesis Documents 1-2 (quick fixes)

These itemize what Addendum 1's to-do 3 ("align both templates to the prompt") must cover beyond the filed heading/Full-question/attribution mismatch — the alignment is incomplete without them.

| # | Sev | Where | Issue | Fix | Levels |
|---|-----|-------|-------|-----|--------|
| 59 | 🟡 | `templates/synthesis-attributed.md:32-33`, `templates/synthesis-merged.md:29-30` vs `prompts/phase4_full-synthesis.md:156-158,215-216` | Both templates end at the question pattern with **no footer block**; the prompt mandates one per document — and they differ (Doc 1: Questions addressed / Personas contributing / Total synthesized insights; Doc 2: Questions addressed / Synthesized insights). The counts are the only self-check that a cluster wasn't truncated, and `build-synthesis.sh` concatenates verbatim so SYNTHESIS.md loses them too. Confirmed: example files carry the prompt's footers. | Add each document's footer (preceded by `---`) to its template, matching the prompt verbatim — two variants, not one shared footer. | med/high |
| 60 | 🟡 | `templates/synthesis-attributed.md`, `synthesis-merged.md` (no mention) vs `prompts/phase4_full-synthesis.md:97` | The `[User Q]`-marker preservation rule ("preserve on the `Full question:` line; do not propagate onto bullets") is absent from both templates — which also lack the `**Full question**:` line it rides on. A template-literal subagent drops user-question traceability, and no gate catches it (SKILL.md:239 checks QUESTIONS.md only). Confirmed: example synthesis files carry `[User Q]` on Full-question lines. | Include the marker rule (and an inline `[User Q]` on the example Full-question line) when applying the to-do 3 alignment. | med/high |
| 61 | 🟡 | `templates/synthesis-merged.md:3` vs `prompts/phase4_full-synthesis.md:99,201` | The merged template describes only structural removal ("sub-bullets … and the `*[Persona Name]*` attribution are removed") — nothing forbids persona names in the synthesized prose itself, which the prompt prohibits in detail (sentence-subject mentions, narrative characters, bullet labels). A template-literal "The Pragmatist warns…" leaks into SYNTHESIS.md, and the humanizer preserves named persons. Confirmed: example merged docs are persona-name-free and rewrite "personas converge" → "perspectives converge", following the prompt rule the template omits. | Extend merged:3 with the prose-level prohibition (breadth via convergence counts and lens types). | med/high |
| 62 | ⚪ | `templates/synthesis-attributed.md:20-33`, `synthesis-merged.md:18-30` vs `prompts/phase4_full-synthesis.md:126,154-155,189,212-213` | Templates omit all three `---` positions (after H1, between question blocks, before footer). No mechanical consumer breaks (`build-synthesis.sh` tolerates body rules; the chunker splits on headings), but template-shaped and prompt-shaped files render differently. | Add the three `---` positions during the alignment. | med/high |
| 63 | ⚪ | `templates/synthesis-attributed.md:28-30`, `synthesis-merged.md:26-27` vs `prompts/phase4_full-synthesis.md:144,162,343` | Consensus-first ordering ("most agreement first … unique perspectives last") appears three times in the prompt, never in the templates — a template-literal file emits insights in arbitrary order, weakening the convergence-count discipline the summary doc inherits. | One bracketed note per template: "ordered by consensus, most convergent first; unique insights last." | med/high |
| 64 | ⚪ | `templates/synthesis-attributed.md:12,20`, `synthesis-merged.md:10,18` vs `prompts/phase4_full-synthesis.md:116,124,179,187` | Fill-slot spelling drift: `effort: "[medium\|high]"` vs the prompt's `"[medium/high]"`; H1 `[Topic Cluster]` vs the prompt's `[Topic Name]` (the former is readable as the slug; examples use the prose name). Frontmatter schema is otherwise congruent — no drift in the 9 fields or `synthesis-type` values. | Normalize both slots to the prompt's spelling. | med/high |

### Scope extensions to already-filed items (no new numbers)

| Extends | Addition |
|---|---|
| #1 | The new "Humanizer pass \| Haiku" Model-Selection row must be scoped to modes (a)/(b) — mode (c) runs inline on the Phase 5 model (see #52). |
| #6 | The same stale topic-area counts also live at `guidance/phase1_effort-level.md:53,86,122` — patch in the same commit or the drift reopens. |
| #17 | Template-held-token half also covers `templates/brainstorm.md:33` (`{{session}}`) — extend phase5:98's carve-out to "all `{{…}}` tokens inside the template are slots you fill (`{{session}}`, `{{current_datetime}}`, `{{cluster_slug}}`)". `humanizer-pass.md:3`'s instance is #51. |
| #21 | The model-requested bracket-slot convention bug is not min-only: identical slots at `phase2B_question-gen_by-persona.md:66`, `phase3_brainstorm_by-persona.md:72`, `phase4_full-synthesis.md:118,181,238`. The spawn sites' "include the literal string" mechanism leaves a weak subagent holding two contradictory sources. Fix all sites together. |
| B3 | `templates/brainstorm.md:15` (medium/high word budgets for Conspicuous Absences) and `:56` (sourcing instruction bracketing only min/low) must travel with B3's resolution — whichever option wins, the template currently requires the section at medium/high while defining its content only for min/low. Confirmed: all 20 medium/high example sessions fabricated the section from no substrate. |
| B6 | Two more default-effort sites beyond those filed: `guidance/phase1_effort-level.md:158` ("If still uncertain → MEDIUM EFFORT (safe default)") and `examples/CHOOSING-AN-EFFORT-LEVEL.md:11,~63` ("medium (default)"; "If you're not sure which level to pick, pick this one"). The guidance site is live: prompt:139 sends uncertain users straight into the document naming the opposite default. Resolve all five sites to one value. |

### B4 amendment (2026-06-11): mode (b)'s transport contract, not just its assembler, is the problem 🔴

New evidence beyond B4 as filed: (1) The text-in/text-out contract (`SKILL.md:354` "handed one `### Question N` block as text … returns its humanized block as text — it writes nothing"; `humanizer-pass.md:8` "do **not** write any file") forces every `_synthesis.md` plus all humanized blocks through the **orchestrator's context** — at high effort tens of thousands of tokens, directly contradicting the paths-not-contents architecture doctrine. (2) The only published high-effort run violated the contract's letter: `examples/youth-mentorship/symphony_high-effort/PLAN.md:22` records "split → **in-place edit on isolated temp blocks** → deterministic reassembly" — subagents wrote files. (3) Neither spec says whether the frontmatter/H1 preamble and the trailing footer are held out of the chunking or ride inside the first/last blocks, where the footer's count labels are unprotected by the preserve list (`humanizer-pass.md:33-41`).

Revised recommendation — codify what the capable run actually did, superseding option (a)'s orchestrator-splices-in-context approach: a deterministic split step writes per-question block files; one Haiku subagent per block edits **its file in place** (mode (b) redefined as path-based, consistent with modes (a)/(c)); a deterministic reassembly step holds preamble/footer out verbatim and writes each `_synthesis.md` once. Whether split/reassembly is an orchestrator shell step or a `scripts/` pair is the remaining sub-choice (original options (a) vs (c)); option (b) (LLM assembler) and the current text-transport contract are both contraindicated by the evidence above.

### Section B — new decision points

### B13. Roster Volume cell `K of N–M` is read as a generation target 🟡
The by-persona 2B prompt delegates volume to "the range listed for your persona" in the Step 2.1 roster (`phase2B_…_by-persona.md:17,21`), but rosters emit quota-bearing cells like `3 of 5-8` (confirmed: `examples/tool-library/symphony_low-effort/PLAN.md:58-61`) — the `3` is the **Phase 2C append quota** (`phase2C:212`), not a generation target; the 2A prompt's Volume Ranges table has no Low column (already flagged in Coverage notes), so the 2.1 subagent improvises the cell format. All four low-effort Append personas generated exactly 3 questions (`volume: 3` frontmatter) — the 2C round-robin then has no selection pool, and a literal 2C agent permanently trips the Step 5 `Q1,Q2,Q3` guard (every 3-question selection is necessarily source-order) — an instruction loop. Contrast: the medium example's plain `5-8` cells yielded 7 questions.
- **(a) Recommended: fix both sides.** Add a parsing rule to the 2B prompt ("if the Volume cell reads `K of N-M`, generate N–M; K is the downstream selection quota") **and** have Step 2.1 emit plain generate-ranges in the Volume column, quota in the Stream column (`Append (3 round-robin)`), folding in the Coverage-notes Low-column fix.
- (b) Hardcode Perspective personas to 5–8 in the 2B prompt's Task section (ignores the roster; simplest, but breaks if ranges ever become persona-specific).
- (c) Fix only the 2.1 cell format (leaves the parsing ambiguity for any nonstandard roster already on disk; resume hazard).

### B14. Phase 3 response headings: the join key is on the wrong sibling 🟡
The generic (min) prompt numbers its question headings (`## Question 1: [Full question text]`, `…generic.md:76`) for a consumer that reads **one** file; the by-persona prompt forbids numbering and uses subagent-derived short summaries (`…by-persona.md:58-59`) for the full-synthesis consumer that must align the same question across **4-7 files** (`phase4_full-synthesis.md:28`). Confirmed drift in derived headings: "Volunteer Spark And The Model Worth Copying" vs "…and the Model…" (tool-library low), "The Anti-Gamification Thesis Under Pressure" vs "Anti-Gamification Thesis…" (habit-tracker low) — same question, different heading text in every pair examined. A weak synthesizer has no stable key and can misattribute responses across questions, corrupting convergence counts.
- **(a) Recommended:** number the by-persona headings (`## Question N: [Short Summary]`) and require the short summary be copied verbatim from the questions file's bold label, not re-derived.
- (b) Keep unnumbered but mandate verbatim label copy (byte-identical headings across persona files; weaker — still no ordinal).
- (c) Instruct phase4_full-synthesis to align by document order only, never by heading text (no producer change; trusts every persona file to cover every question in order).

### B15. min question-heading format: Title Case rule is unsatisfiable against full-text headings 🟡
`phase3_brainstorm_generic.md:52` ("Use Title Case for all headings") was carried verbatim from the by-persona sibling while the heading content changed to full question text (`:76`) — a sentence-length heading cannot both reproduce the question and be Title Cased. Confirmed: two capable runs resolved it in opposite directions (tool-library min keeps full text in sentence case; habit-tracker min invents `## Question 1: Anti-Gamification as Identity` + an italic full-question paragraph not in the template). Stakes: the min Phase 4 prompt's inputs do not include the questions file, so if the heading drops the full text, the synthesizer loses the question wording (the min-workflow Step 3 bullets say otherwise — that desync is filed #20).
- **(a) Recommended:** scope the rule — "Title Case for section headings; question headings reproduce the question text verbatim (sentence case)." Keeps full text where the consumer needs it; minimal change.
- (b) Adopt `## Question N: [Short Label]` + a mandatory italic full-question line beneath (codifies the habit-tracker run; template change, prettier headings).
- (c) Drop the Title Case bullet from generic entirely (loses heading discipline for section headings too).

### B16. min's BRAINSTORM word-budget column is internally unsatisfiable 🟡
At min, fixed-section budgets sum to 690–1310 and per-topic blocks add 80–160 × **3-5 clusters** (`min-effort-workflow.md:44`), so the maximum respecting every ceiling is 2,110 — under the 1600–2500 aggregate band in a way that makes the template's own advice wrong twice: "default to the lower end of each range" lands at ~930–1090, 500+ under the floor that audit (a) (`phase5:52`) then declares incomplete, and "a draft that hits the upper end of every range will exceed the aggregate band" (`brainstorm.md:20`) is arithmetically false at min. A weak model oscillates between "cut, section over cap" and "expand, aggregate under floor." Confirmed: every sampled min example passes the floor only by blowing through per-section ceilings (tool-library min: Exec 337/cap 300, Themes 377/350, Absences 213/150, Next Steps 390/240). The template's `:22` worked example assumes 10 clusters and is never qualified per column.
- **(a) Recommended:** lower min's aggregate band to match its per-section budgets × 3-5 clusters (≈1000–2000), state each column's assumed cluster count under the table, and effort-qualify the line-20 upper-end warning.
- (b) Raise min's per-section ranges ~50% so the existing band is reachable (lengthens min, against its speed-run character).
- (c) Make the aggregate band derived ("sum of your per-section targets") instead of a fixed row (removes the independent cross-check the audit step uses).

### B17. Wall-clock estimates: one orphaned figure, and med/high promises ~2× optimistic against measured scaling 🟡
`guidance/phase1_effort-level.md:52`'s "~10-15 minutes" matches no level (min: 5-10, low: 15-20 — an orphan of the three-level era, fixed by #54). Separately, the medium/high estimates both files quote to the user (~20-30 / ~45-60 min, prompt:61,98 = guidance:85,121) sit well below what the project's own measured data implies: `examples/CHOOSING-AN-EFFORT-LEVEL.md:14,20` puts relative cost at 1×/3.3×/8.3×/14.6× and says "time on task scales roughly in line with the token figures" — anchored to min's 5-10 min, that implies medium ~40-80 and high ~75-145 min. No direct wall-clock measurements exist in the repo, so this is inference, not confirmation.
- **(a) Recommended:** measure wall-clock from the 40 example sessions (PLAN.md timestamps) and update prompt templates + guidance to one sourced set of ranges.
- (b) Keep current estimates but qualify the CHOOSING guide's "time scales in line with tokens" sentence (parallel subagents make time sublinear in tokens) — cheapest, defensible if the estimates were ever empirical.
- (c) Drop minute-estimates from the Phase 1 user scripts entirely and point at the relative-cost table.

### Addendum 2 coverage notes

- **Per-target verdicts:** Phase 2B siblings — 6 findings + B13 (shared core congruent; residue concentrated in roster-volume coupling and min spawn-site duplication). Phase 3 siblings — 4 findings + B14/B15 (+ one duplicate of #35 correctly excluded). Phase 5 paths — 2 findings + B16 + B3/#17 extensions. Humanizer call sites — 5 findings + B4 amendment (mode (a) wording and build-script re-sequencing fully congruent across levels). Phase 1 — 5 findings + B17 + #6/B6 extensions (the guidance is structurally pre-`min`; HIGH/MEDIUM trigger lists verbatim-identical). Synthesis templates — 6 findings (frontmatter schema and `synthesis-type` values congruent; everything else in the question-block anatomy drifted).
- **Pattern worth naming:** the min spawn-site instruction-duplication failure (#20) is systemic, not local — confirmed at Steps 1 (#38), 2 (#46), and 3 (#20). The fix direction is uniform: delegate to the prompt file, keep only spawn mechanics, as SKILL.md's own Phase 3 sections already do.
- **Verified clean (for the record):** 2B siblings' Coverage Requirements / Quality Standards / numbering rules; phase3 isolation rules; humanizer mode (a) invocation wording at all four sites; HIGH/MEDIUM Phase 1 trigger lists; min/low template numbers vs SKILL.md and min-workflow; synthesis-template frontmatter schemas.

---

## Addendum 3 (2026-06-11): Batch execution record

Six parallel implementer agents (one per file group, disjoint write sets) executed B1 plus all decision-independent quick fixes; cross-agent seams (substitution-variable conventions) were verified and closed by the orchestrating session. 24 skill files changed.

**Implemented:**
- **Decision B1** — min Step 4 delegates to `prompts/phase5_final-output.md` (Sonnet, `{{effort}}`="min", NotebookLM flag never present); inline content list, Session-Index pruning bullet, em-dash paragraph, and standalone humanizer paragraph deleted.
- **Quick fixes #1–14, #17–35** (vertical) — with #16 applied for its Inputs-list half (the min carve-out phrasing stays open under B2) and #13's Step 2.4 substitution line deliberately skipped (travels with B5).
- **#36 partial** — provenance stripped from SKILL.md, phase1 prompt (narration only), phase2A prompt/guidance, phase2D guidance, 2B generic, 2C (`BL1` label dropped, CLAUDE.md pointers inlined). **Not** stripped: phase2C R-labels (open B11) and `guidance/phase1_effort-level.md` (whole file travels with #54).
- **Quick fixes #37–53, #59–64** (horizontal) — including the #1+#52 pairing (Haiku row scoped to modes (a)/(b)).
- **Addendum 1 to-dos 1 and 3** — synthesis-summary template now documents min's Conspicuous absences + low's Neither-lens gaps; attributed/merged templates fully aligned to full-synthesis Documents 1–2 (headings, Full-question lines, em-dash attribution, footers, separators, ordering note, slot spellings).
- **Convention sweep beyond the cited sites** (same defect class, found at seam-check): `phase4_summary-only_low-effort.md:71` and `templates/synthesis-summary.md` model/effort slots → `{{model_requested}}`/`{{effort}}`; `templates/brainstorm.md:35` + `templates/notebook-lm-instructions.md:64` effort slots → `{{effort}}` (phase5 carve-out extended to match); `{path}` → `{{path}}` in SKILL.md and humanizer-pass.md (closing the original coverage-note claim). `templates/plan.md`'s bracket slots intentionally kept — PLAN.md is orchestrator-written.
- **Notable judgment calls:** Step 2.1's literal-string model mechanism kept as-is (phase2A's slot was not in #21's scope; the pair stays internally consistent). 2C's frontmatter `effort`/`model-requested` slots converted to `{{…}}` (Step 2.3's new Substitution line resolves both).

**Still open (deferred to decisions, resume at B2):**
| Item | Waits on |
|---|---|
| #15 (Step 2.4 must-add content + its #13 substitution line + exactly-7 reconcile step) | B5 |
| #16 min carve-out phrasing vs the no-re-read rule | B2 |
| #36 phase2C R-labels | B11 |
| #54–58 + #6/B6 guidance-side extensions + B17 (one guidance-file rewrite job) | B6 (and B17) |
| Coverage-note Tier-3-at-low + Volume-table Low column | B13 (folds into its option (a)) |
| Undecided: B2, B3, B4 (amended), B5–B17 minus B13-independent items | — |

---

## Companion artifact

Reconciled per-effort-level workflow flowcharts: [docs/workflow-flowcharts.md](../docs/workflow-flowcharts.md).
