# USER-QUESTIONS.md Template

Optional file capturing specific questions the user wants the brainstorming process to answer. Persona-based question generators should NOT see this file — only the `min` generic generator (Phase 2 min workflow) and the `low`/`medium`/`high` synthesizer (Phase 2 Step 2.3) read it.

```markdown
# User-Defined Questions

Questions the user wants the brainstorming process to answer.

1. [Question text]
2. [Question text]
...
```
